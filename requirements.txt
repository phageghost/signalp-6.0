# Core scientific computing packages
scikit-learn>=1.0.0
scipy>=1.7.0
seaborn>=0.11.0

# Deep learning packages
torch>=1.7.0
tokenizers>=0.9.0

# Utility packages
tqdm>=4.46.0
typing-extensions>=3.7.4

# Note: sklearn is an alias for scikit-learn, so we don't need both
# Note: typing package is included in Python 3.5+, so we don't need it separately